mod metric;
use metric::*;
mod BKTree;
use BKTree::*;
use std::Vec;
use std::collections::Hashset;

// will need functions to load dictionaries
// Dryad struct ? 


pub struct Dryad {
    tree: BKTree,
}


fn main() {
    println!("Hello World!");
}