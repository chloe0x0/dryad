mod metric;
use metric::*;
use std::Vec;
use std::collections::Hashset;

// will need functions to load dictionaries
// Dryad struct ? 


pub struct Dryad {
    Dictionary: Hashset<String>,
    Metric: 
}


fn main() {
    println!("Hello World!");
}